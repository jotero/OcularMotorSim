"""Benchmark suite — validation scripts that exercise the oculomotor model.

Run one via:  python -X utf8 -m oculomotor.benchmarks.bench_saccades
Figures are written to the repo-level web/ cache (web/benchmarks/figures, etc.).
"""
