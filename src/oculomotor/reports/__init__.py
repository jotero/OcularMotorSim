"""Report generators — build the static web/ cache (pre-rendered docs + benchmark galleries).

Run via, e.g.:  python -X utf8 -m oculomotor.reports.run_benchmarks
                python -X utf8 -m oculomotor.reports.gen_parameters
Outputs are written into the repo-level web/ cache.
"""
